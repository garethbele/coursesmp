name: Deploy Django to Render with Docker

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Build Docker image
        run: docker build -t coursesmp .

      # Optionally, push to a registry if Render is configured to pull from GitHub
      # For Render’s GitHub Auto-Deploy, you can skip this step entirely
